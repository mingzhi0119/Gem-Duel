# Testing Index

## ZH

新的测试基线已经迁移到 `vitest + turbo` 的 monorepo 模式。请优先参考：

- [`docs/00-refactor/full-rebuild-plan.md`](./docs/00-refactor/full-rebuild-plan.md)
- `packages/contracts/src/__tests__`
- `packages/core-engine/src/__tests__`

旧版测试说明不再作为架构真相；legacy 测试语义请结合 `old/legacy-vite-electron/src/logic/__tests__` 与 git 历史查看。

## EN

The new testing baseline now follows the `vitest + turbo` monorepo setup. Start with:

- [`docs/00-refactor/full-rebuild-plan.md`](./docs/00-refactor/full-rebuild-plan.md)
- `packages/contracts/src/__tests__`
- `packages/core-engine/src/__tests__`

Legacy test guidance is no longer architecture truth; consult `old/legacy-vite-electron/src/logic/__tests__` and git history when needed.
