{
    "name": "@gem-duel/desktop",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist/main.js",
    "scripts": {
        "dev": "tsx watch src/main.ts",
        "build": "tsc -p tsconfig.json",
        "lint": "eslint src --ext .ts",
        "test": "vitest run --passWithNoTests",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "electron": "^39.2.3",
        "electron-log": "^5.4.3",
        "electron-updater": "^6.6.2"
    }
}
