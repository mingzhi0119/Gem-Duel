import { Section } from '@gem-duel/ui';

export default function SettingsPage() {
    return (
        <Section title="Settings">
            <p className="gd-muted">
                ZH: 这里将承接跨端设置、实验开关和房间服务端点配置。 EN: This route will host
                cross-platform settings, feature flags, and room-service endpoint configuration.
            </p>
        </Section>
    );
}
