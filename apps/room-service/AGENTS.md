# Room Service Rules

- This app owns authoritative online orchestration, room lifecycle, and transport coordination.
- Match truth must still come from contracts + application + core-engine, not ad hoc route logic.
- Keep protocol evolution documented in `docs/30-contracts/` and operational behavior documented in `docs/40-operations/`.
