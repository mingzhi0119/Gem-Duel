{
    "name": "@gem-duel/core-engine",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "dependencies": {
        "@gem-duel/contracts": "workspace:*",
        "@gem-duel/domain": "workspace:*",
        "xstate": "^5.23.0"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "eslint src --ext .ts",
        "test": "vitest run src/__tests__ --passWithNoTests",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    }
}
