{
    "name": "@gem-duel/adapters",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "dependencies": {
        "@gem-duel/contracts": "workspace:*",
        "@gem-duel/core-engine": "workspace:*",
        "@gem-duel/domain": "workspace:*",
        "pure-rand": "^8.4.0"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "eslint src --ext .ts",
        "test": "vitest run --passWithNoTests",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    }
}
