#!/usr/bin/env node

console.log('Replay capture placeholder');
console.log('Intended use: capture a MessagePack replay fixture once the engine wiring exists.');
console.log(
    'Required metadata: schemaVersion, rulesetVersion, engineVersion, seed, commands, events, finalStateHash.'
);
